If you think you've found a bug in PHP3, you can report it on the bug
reporting page at:
	http://www.php.net/

Current Known Bugs:

* split() function doesn't return regex errors nicely
* Preprocessed scripts don't work with require()
